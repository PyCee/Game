/**
 * Sound
 * Video
 * Controls
 * 
 * 
 * 
 * 
 * 
 * 
 * 
 * 
**/

#ifndef _OPTIONS_
#define _OPTIONS_

void InitOptions();

void SaveOptions();

void ResetAllOptions();

void ResetAllSoundOptions();

void ResetSoundLevel();
char SoundLevel;
char MuteSound = 0;
#define DEFAULT_SOUND_LEVEL 0
#define MAX_SOUND_LEVEL 1
#define MIN_SOUND_LEVEL 0

void ResetMusicLevel();
char MusicLevel;
char MuteMusic = 0;
#define DEFAULT_MUSIC_LEVEL 0
#define MAX_MUSIC_LEVEL 1
#define MIN_MUSIC_LEVEL 0

void ResetEffectsLevel();
char EffectsLevel;
char MuteEffects = 0;
#define DEFAULT_EFFECTS_LEVEL 0
#define MAX_EFFECTS_LEVEL 1
#define MIN_EFFECTS_LEVEL 0

void ResetAllVideoOptions();

void ResetFOV();
char FOV; // Field of View, use this + 50 or so, to get 50-178 (more than enough)
#define DEFAULT_FOV 100
#define MAX_FOV 178
#define MIN_FOV	50

void ResetBrightness();
char Brightness = 1;
#define DEFAULT_BRIGHTNESS 1
#define MAX_BRIGHTNESS 1
#define MIN_BRIGHTNESS 0

void ResetAllControlOptions();

void ResetMouseSensitivity();
char MouseSensitivity;
#define DEFAULT_MOUSE_SENSITIVITY 1
#define MAX_MOUSE_SENSITIVITY 1
#define MIN_MOUSE_SENSITIVITY 0

#endif
