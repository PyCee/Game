
#ifndef _GAME_WORLD_
#define _GAME_WORLD_


typedef struct _GameWorld_t{
	F64 Height;
	F64 Width;
	F64 Length;
} GameWorld_t;

void genGameWorld(Game_World_t **);
void freeGameWorld(Game_World_t **);

#endif /* _GAME_WORLD_ */
